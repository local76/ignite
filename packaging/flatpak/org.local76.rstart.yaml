app-id: org.local76.rstart
runtime: org.freedesktop.Platform
runtime-version: '23.08'
sdk: org.freedesktop.Sdk
command: rstart
modules:
  - name: rstart
    buildsystem: simple
    build-commands:
      - cargo build --release --locked
      - install -Dm755 target/release/rstart /app/bin/rstart
